Cub3D study steps:

- Create .h for structs player map and data
- Parsing .cub to t_data with map checks(if player surround by wall,
                                         if there are more than 1 player)
    - t_map
        - init data (path to texture of all directions, color of floor and ceilling, map)
    - t_player
        - init data (player position, vecteurs(direction and plane))
- Rendering
    - create custom mlx_pixel_put (see Mlx42docs)
    - draw a squar with custom mlx_pixel_put
    - draw a squar for player
    - rend player and wall
    - key hook config for mouvement
    - rend 3dmap (lodev raycasting)
        - init all element for DDA 
            - cameraX, ray directoin, delta distance, side distance
            - steps, side, map position
        - perform DDA
        - une fois le mur hit par le rayon, on a la distance entre le joueur et le mur(side distance)
        - calculate -> calculatla hauteur du mur et designe aux drawStart et drawEnd(draw colone)
    - rend wall texture
        - creer un struct de texture (addr, width, height, bpp, line_length, endian)
        - charger texture.xpm et transformer en img utilisable (mlx_xpm_file_to_image)
        - donne l'addresse a l'image (mlx_get_data_addr)

        - calculate TextureX => quelle colone de la texture affiche pour la colone sur l'ecran
            - 